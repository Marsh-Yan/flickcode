# FlickCode 上下文管理 Checklist

> 每项都必须通过可观察行为、测试输出或生成文件验证；不以阅读实现代码作为验收依据。

## 实现完整性

- [ ] **统一预检**：用记录调用的 FakeProvider 分别驱动 `Session.chat()` 与至少两轮 `AgentLoop.run()`；观察每次主 Provider 请求前都经过同一个上下文预检，且未超阈值时不额外产生摘要请求。
- [ ] **单工具结果存盘**：让工具输出超过单结果阈值；观察完整原文写入 FlickCode 上下文目录，历史只保留含工具名、调用 ID、原始长度和可读取路径的预览；读取路径能得到完整原文。
- [ ] **工具批次存盘**：构造一个 assistant tool-call 与多个 tool result，令批次合计超阈值；观察系统按结果大小降序替换，最大结果最先存盘，assistant/tool 配对和 tool call ID 仍合法。
- [ ] **用户内容保真**：在任意轻量预防和重量摘要场景中，对比原始 user `Message`；确认未被截断、替换或静默删除。
- [ ] **近似估算**：提供一次主请求 usage，再追加消息；观察估算以 usage 为锚点，只对增量字符估算。修改早期历史后，观察锚点被重建而非套用失效增量。
- [ ] **安全余量**：在相同历史下比较两种模式；自动预检使用 13K 余量，手动 `/compact` 使用 3K 余量，且未使用精确 tokenizer。
- [ ] **历史摘要**：构造逼近上下文窗口的消息历史；观察早期内容由一条有固定六段结构的摘要替代，尾部约 10K Token 的近期原文仍在，且至少保留 5 条消息。
- [ ] **边界消息**：摘要后观察紧随摘要存在普通 user 消息，要求需要文件细节时重新读取存盘文件，且不得依据摘要臆测代码细节。
- [ ] **摘要调用隔离**：检查 FakeProvider 的摘要调用参数；观察 `tools=None`，只接收一条序列化历史的 user 消息和专用 Prompt，摘要调用不会触发主预检或写入主历史。
- [ ] **摘要 Prompt**：检查摘要 Prompt 明确禁止工具调用、要求内部分析草稿后仅输出正式摘要、草稿不得进入历史/文件/结果，并规定六个固定章节；缺任何章节的响应被判定为失败。
- [ ] **失败重试和熔断**：让摘要连续失败 3 次；观察每次原因可诊断，第 3 次后熔断，后续自动预检和 `/compact` 不再调用摘要 Provider。
- [ ] **熔断恢复**：在摘要成功或调用显式 reset 后，观察失败计数清零、熔断关闭，后续请求允许再次摘要。
- [ ] **失败数据保护**：模拟工具结果存盘失败、摘要失败、压缩后仍超预算；确认原工具原文和 user 消息仍在，或主请求明确被安全阻断，无静默删除。
- [ ] **可诊断性**：触发存盘、摘要、重试、熔断和阻断，观察诊断中提供估算值、窗口、余量、状态原因和生成文件路径；终端不重复打印大段工具原文。

## 集成

- [ ] **普通对话路径**：通过 `Session.chat()` 触发大工具结果和接近窗口的历史；确认 Provider 得到处理后的合法消息，done usage 回写到同一会话级上下文管理器。
- [ ] **Agent Loop 路径**：使用两轮工具调用 Agent 场景；确认每轮 Provider 请求前预检，压缩同步到 `Session.messages`，临时 whisper 不进入保存的历史或摘要。
- [ ] **工具与权限不回归**：执行读工具批量、写工具串行、权限拒绝和未知工具场景；观察执行顺序与拒绝语义不因上下文管理而改变。
- [ ] **Provider 兼容**：将压缩后的历史分别送入 Anthropic 和 OpenAI 的消息转换器；观察没有拆开的 assistant/tool 配对、没有孤立 tool 结果，生成的请求结构合法。
- [ ] **配置兼容**：加载没有 `context` 节的旧配置，观察默认值生效；加载完整 context 配置，观察窗口、阈值、目录、字符换算覆盖成功；非法配置返回明确错误。
- [ ] **TUI `/compact`**：在交互模式输入 `/compact`，即使未达到自动阈值也执行压缩，并显示结果、文件路径或熔断/阻断原因；`/plan`、`/do`、`/exit`、`/quit` 不受影响。
- [ ] **管道模式兼容**：通过标准输入发送普通消息；观察仍使用 `Session.chat()` 并正常输出，不需要交互式 `/compact` 支持。

## 编译与测试

- [ ] **上下文测试**：运行 `uv run python -m unittest tests.test_context -v`，预期所有上下文单元和 FakeProvider 集成测试通过，且没有真实网络调用。
- [ ] **项目回归**：运行 `uv run python -m unittest discover -s tests -v`，预期现有 MCP、Agent Loop、工具与权限测试全部通过。
- [ ] **导入与编译**：运行 `uv run python -m compileall -q src tests`，预期退出码为 0。
- [ ] **文档一致性**：阅读 README 的 `/compact`、`context` 配置和存盘目录说明；实际运行与文档一致。

## 端到端场景

- [ ] **场景 1：大工具结果恢复**
  1. 使用 FakeProvider 让 Agent 调用读取工具，返回超过阈值的完整文件输出。
  2. 继续下一轮请求。
  3. 观察工具原文已存盘，下一轮只收到预览和路径。
  4. 让模型重新调用读取工具获取细节，观察读取成功而不依赖摘要猜测。

- [ ] **场景 2：自动历史压缩**
  1. 构造含用户需求、工具结果、关键决策和近期工作进度的长历史。
  2. 设置窗口，使自动请求预算不足。
  3. 发起普通对话或 Agent 下一轮请求。
  4. 观察摘要包含六段信息、边界消息存在、近期至少 5 条原文保留，并能正常继续请求。

- [ ] **场景 3：手动压缩与熔断**
  1. 在未达到自动阈值的会话输入 `/compact`，观察按 3K 余量完成压缩。
  2. 将摘要 FakeProvider 改为持续失败，再连续触发压缩。
  3. 观察第 3 次失败后熔断，后续自动和手动压缩均不再调用摘要模型，并明确说明阻断或可继续状态。
