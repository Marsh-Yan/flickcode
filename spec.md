# FlickCode 上下文管理 Spec

## 背景

FlickCode 当前支持普通对话和 Agent Loop 两条 Provider 请求路径：

- 普通对话通过 `Session.chat()` 请求 Provider；
- Agent Loop 通过 `AgentLoop.run()` 多轮请求 Provider。

两条路径都会持续累积 `Message` 历史。Agent Loop 还会不断追加 assistant、tool call、tool result 和 thinking 消息。其中工具结果可能包含完整文件内容、搜索结果或命令输出，是上下文 Token 的主要来源。

随着对话持续进行，消息历史可能逼近模型上下文窗口，最终导致 API 请求失败或 Agent 无法继续工作。当前实现缺少单条工具结果保护、累积历史压缩、压缩产物存盘、用户手动触发和摘要失败熔断能力。

## 目标

为 FlickCode 增加统一的上下文管理能力，并接入 `Session.chat()` 与 `AgentLoop.run()` 的每一次 Provider 请求前流程。

每次请求前按以下顺序处理上下文：

1. 轻量预防：优先压缩或存盘过大的工具结果；
2. 整体判断：使用近似 Token 估算和安全余量判断是否需要历史摘要；
3. 重量兜底：摘要较早消息，保留近期原文；
4. 在压缩结果后追加边界提示，要求模型需要文件细节时重新读取存盘文件。

本阶段只采用 API usage 锚点加字符数增量的近似估算，不引入精确 tokenizer，也不做摘要策略的机器学习优化。

## 功能需求

### F1：统一请求前上下文预检

在普通对话和 Agent Loop 的每一次 Provider API 请求前，必须执行同一套上下文预检流程。

预检顺序固定为：

1. 处理单个工具结果过大问题；
2. 处理单条消息内工具结果合计过大问题；
3. 估算整体上下文长度；
4. 必要时执行历史摘要；
5. 将处理后的消息历史发送给 Provider。

如果预检中的存盘或摘要失败仍可安全继续请求，系统应优先保留原消息并继续执行；如果继续请求会明显超过上下文预算，则停止本次请求并向用户报告原因。

### F2：工具结果轻量预防

工具结果进入消息历史前，执行以下规则：

- 单个工具结果超过单结果阈值时，将完整结果存入上下文文件；对话消息只保留短预览、结果来源和文件路径。
- 单条消息中的多个工具结果合计超过消息阈值时，按结果大小从大到小依次存盘，直到该消息低于阈值或没有可处理结果。
- 用户原始消息不得因为轻量预防被改写或摘要。
- 预览必须包含结果来源、存盘文件路径，以及提示模型需要细节时重新读取文件的说明。
- 存盘失败时不得静默丢弃工具结果；原结果必须保留，并记录可诊断错误。

单结果阈值、消息阈值和相关文件目录必须可配置，并提供可测试的默认值；具体配置接口和默认值在技术设计阶段确定。

### F3：近似 Token 估算

上下文管理使用近似估算，不调用精确 tokenizer：

- 以最近一次 API usage 中的输入 Token 数作为估算锚点；
- 后续只对锚点之后新增或变化的消息按字符数进行增量估算；
- 估算结果至少区分当前历史、预留输出空间和安全余量；
- 自动压缩使用约 13K Token 安全余量；
- 用户手动触发压缩使用约 3K Token 安全余量；
- 没有可用 API usage 时，使用当前消息历史建立初始近似值；
- 估算异常或 usage 缺失不得导致无限重试。

上下文窗口预算由调用方或配置提供。模型上下文窗口的在线探测和精确 tokenizer 不属于本次范围。

### F4：整体历史重量兜底

当整体上下文逼近 Provider 的上下文窗口上限时，执行一次重量压缩：

- 从消息尾部按近似 Token 反向保留近期内容；
- 近期内容目标约为 10K Token，且至少保留最近 5 条消息；
- 较早消息替换为一条结构化摘要；
- 用户原始消息在摘要中应尽可能保留原文，不得把用户需求简单改写成失真结论；
- 摘要后追加一条边界消息，明确说明：较早内容已经被压缩，文件细节以存盘文件为准，模型需要细节时必须重新读取文件，不得依据摘要臆测代码细节。

如果保留近期内容后仍无法满足预算，系统应继续按同一规则扩大压缩范围，但不得删除全部消息历史。

### F5：结构化摘要生成

摘要由 LLM 生成，并要求输出固定结构，至少包含：

- 用户目标与明确约束；
- 已完成的工作；
- 关键决策与理由；
- 当前状态、未完成事项和阻塞点；
- 涉及的文件、路径和重要细节；
- 后续建议或下一步。

摘要 Prompt 必须明确要求：

- 不得调用任何工具；
- 先在内部生成分析草稿，再输出正式摘要；
- 草稿不得进入会话历史、文件或最终摘要；
- 只根据提供的历史消息生成摘要；
- 不确定的信息必须标明不确定，不得补写代码细节。

摘要请求必须使用独立的摘要调用语义，不得把摘要过程中的分析草稿或工具调用混入主会话历史。

### F6：摘要失败保护与熔断

摘要请求失败时：

- 最多连续重试 3 次；
- 每次失败都记录可诊断原因；
- 连续失败 3 次后进入熔断状态；
- 熔断期间自动预检不得再次触发摘要请求，避免请求死循环；
- 系统应保留可发送的最小消息集合；若仍超出预算，则中止本次 Provider 请求并提示用户；
- 一次成功摘要或用户明确重置上下文状态后，允许恢复摘要能力。

### F7：用户手动触发

提供 `/compact` 作为用户主动触发上下文压缩的入口。手动触发时：

- 使用约 3K Token 安全余量；
- 复用与自动压缩相同的摘要格式、存盘机制和边界提示；
- 即使当前未达到自动压缩阈值，也可以执行；
- 压缩完成后向用户报告压缩结果、摘要状态及生成的文件路径；
- 熔断状态下，手动触发不得绕过连续失败 3 次的保护逻辑，并应明确报告当前状态。

### F8：两条路径行为一致

普通对话和 Agent Loop 必须共享相同的上下文管理语义：

- 使用相同的工具结果存盘规则；
- 使用相同的 Token 估算规则；
- 使用相同的自动和手动压缩阈值；
- 使用相同的摘要失败熔断；
- 压缩后继续请求时，Provider 仍能接收到合法且顺序正确的消息历史。

## 非功能需求

### N1：用户内容保真

用户原始消息必须作为高优先级上下文保留。自动摘要可以压缩较早消息，但不得无依据改写用户目标、约束、否定条件或明确决策。工具结果可以被预览替换，但完整内容必须可通过存盘路径恢复。

### N2：Provider 与路径兼容

上下文管理器处理统一的内部 `Message` 历史，不依赖某一个 Provider 的专用消息格式。现有 Provider 继续负责 Anthropic 和 OpenAI 的格式转换。

普通对话与 Agent Loop 必须共享同一套管理逻辑，不能因为走不同请求路径而产生不同的压缩结果或失败策略。

### N3：有界开销

每次请求前的轻量预检主要使用本地计算，不额外调用 LLM。只有触发重量兜底时才允许额外发起摘要请求。

摘要请求次数受 3 次连续失败熔断约束；预检不得因估算误差进入无限压缩或重试循环。

### N4：文件安全与可恢复性

工具结果存盘必须使用 FlickCode 管理的上下文文件目录，生成稳定且可追踪的文件路径，保留原始结果内容，不因预览截断而丢失，并避免覆盖用户主动创建的同名文件。存盘失败时保留内存中的原始工具结果；摘要和预览中记录文件路径。

### N5：可诊断性

系统至少能够记录或向用户报告：

- 触发了单结果存盘、消息级存盘或历史摘要；
- 估算的上下文 Token、窗口预算和采用的安全余量；
- 摘要成功、失败、重试和熔断状态；
- 生成的摘要文件或工具结果文件路径；
- 因上下文仍超预算而中止请求的原因。

正常运行时不应把大量工具原文重复打印到终端。

### N6：向后兼容

没有触发阈值时，现有对话历史顺序、Provider 请求行为和工具调用行为保持不变。普通纯文本对话不得因为新增上下文管理而额外产生摘要请求或改变用户可见回答。

### N7：可测试性

上下文估算、工具结果裁剪、消息选择、摘要失败熔断和两条请求路径接入都必须能够通过不依赖真实 LLM 的单元测试验证。摘要 Prompt 的关键约束也必须可被测试检查。

## 不做的事

- 不引入精确 tokenizer；继续使用 API usage 锚点加字符数增量估算。
- 不做基于机器学习的摘要策略、重要性排序或自动调参。
- 不建立跨会话长期记忆系统；存盘文件只服务于当前上下文恢复。
- 不修改 Anthropic、OpenAI 的底层协议转换逻辑；继续由现有 Provider 负责格式适配。
- 不改变工具本身的执行语义、权限策略、并发策略或结果生成逻辑。
- 不删除用户主动创建的文件，也不把上下文存盘文件当作项目源码管理。
- 不在摘要请求中调用工具，不让摘要模型自行读取文件。
- 不保证压缩后保留所有历史原文；但必须保留用户目标、约束、决策、状态和文件路径等可恢复信息。
- 不在本阶段实现跨设备同步、压缩文件清理策略或可视化上下文分析界面。

## 验收标准

- **AC1：两条路径统一预检**：对普通 `Session.chat()` 和 Agent Loop 的每一次 Provider 请求执行同一套预检；上下文未达到阈值时不额外发起摘要请求，正常请求行为保持不变。
- **AC2：单个工具结果存盘**：构造超过单结果阈值的工具输出并继续请求，观察到原始结果被写入上下文文件，消息中只保留预览、来源和可解析路径；读取该文件可恢复完整原文。
- **AC3：单条消息批量存盘**：构造一条包含多个工具结果且合计超过阈值的消息，观察系统按结果大小从大到小依次存盘，并优先替换最大的结果；用户消息内容保持不变。
- **AC4：估算与安全余量**：使用模拟 usage 和增量消息验证估算以最近一次输入 Token 为锚点、增量按字符数估算；自动触发保留约 13K Token 余量，手动 `/compact` 触发保留约 3K Token 余量，过程中不调用精确 tokenizer。
- **AC5：整体历史摘要**：构造接近上下文窗口上限的历史，观察较早消息被一条结构化摘要替换，近期消息从尾部保留约 10K Token 且至少 5 条；消息顺序和 Provider 可接受格式保持合法。
- **AC6：摘要结构与边界提示**：检查摘要包含用户目标与约束、已完成工作、关键决策、当前状态、文件细节和后续步骤；摘要后存在边界消息，明确要求模型需要文件细节时重新读取存盘文件，不得依据摘要臆测代码。
- **AC7：摘要 Prompt 安全约束**：检查摘要请求 Prompt 明确禁止调用工具、要求先生成内部分析草稿再输出正式摘要，并且草稿不进入消息历史、存盘文件或最终摘要。
- **AC8：失败重试与熔断**：使用模拟摘要失败验证最多连续重试 3 次；第 3 次失败后进入熔断，自动预检不再发起摘要请求；手动 `/compact` 也不能绕过熔断，并向用户报告原因。
- **AC9：失败时不丢数据**：模拟存盘失败、摘要失败和压缩后仍超预算，观察原始工具结果仍可保留或请求被安全中止；系统不得静默删除用户消息或工具结果。
- **AC10：手动压缩入口**：在交互式 TUI 中输入 `/compact`，即使未达到自动阈值也能执行上下文压缩，并报告是否压缩成功、摘要状态及生成的文件路径。
- **AC11：兼容现有行为**：运行纯文本对话、普通工具调用、Agent Loop 和管道模式，确认未触发阈值时不产生额外摘要请求，工具调用顺序与既有 Provider 适配保持正常。
- **AC12：可诊断性**：通过日志或用户可见提示确认能够区分存盘、摘要、重试、熔断和超预算中止，并能定位对应的上下文文件。
